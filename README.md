g++ src/*.cpp -Iinclude -o bin/virtualDisk && .bin/virtualDisk


NOTE FOR TOMMOROW
- fix found bugs
  1. when running cd ./ on root it displays double roots
- make ls handle listing of folders too
- work on input validation for ls, cd and mkdir before proceeding on working to other commands
  and handling edge case scenarios